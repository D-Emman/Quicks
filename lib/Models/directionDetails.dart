class DirectionDetails{
  var distanceValue;
  var durationValue;
  var distanceText;
  var durationText;
  var encodePoints;

  DirectionDetails({this.distanceText,this.distanceValue,this.durationText,this.durationValue,this.encodePoints});
}